#include <stdio.h>

int main() {
        int x = 5;
        int y = 0;
        int z;
        if (x == 5) {
                y = 1;
        }

        for (int i=0; i<1; i++)
                x++;

        for (int i=0; i <= x; i++) {
                int loop =0;
                z += 10;
        }

        while (y < 5) {
                printf("trapped forever");
        }

        return 0;

}

int f(int x, int y) {
        return x+y;
}
